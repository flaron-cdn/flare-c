# hello

Smallest possible Flaron flare in C: returns `hello from flare-c` as
`text/plain` for any HTTP request.

Build (from the repo root):

```sh
make examples
```

Produces `build/examples/hello.wasm`. Deploy via the Flaron dashboard or
`flaronctl`.
